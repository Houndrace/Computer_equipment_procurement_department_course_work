using ProcurementDepartment.Models.ProcurementModel;
using ProcurementDepartment.Views;
using System;
using System.Collections.Generic;
using System.Configuration;
using System.Linq;
using System.Text;
using System.Threading.Tasks;
using System.Windows.Input;
using System.Windows.Navigation;

namespace ProcurementDepartment.ViewModels
{
    public class MainViewModel { } 
       /* private string _fullName;
        public string FullName
        {
            get => _fullName;
            set
            {
                _fullName = value;
                OnPropertyChanged(nameof(FullName));
            }
        }

        public Сотрудник Employee { get; }

        public MainViewModel(Сотрудник employee) 
        {
            Employee = employee;
            FullName = getFullName();

            OrdersViewModel ordersViewModel = new OrdersViewModel(Employee);
            NavigateToOrdersCommand = new RelayCommand(() => { });
        }

        public ICommand NavigateToOrdersCommand { get; private set; }

        */
    
}
